# Host to alias z ~/.ssh/config — użytkownik, klucz i IdentitiesOnly są tam
# (bez IdentitiesOnly serwer zrywa połączenie: "Too many authentication failures").
[prod]
kchroscinski-prod

[prod:vars]
ansible_user=mchalt
ansible_python_interpreter=/usr/bin/python3
